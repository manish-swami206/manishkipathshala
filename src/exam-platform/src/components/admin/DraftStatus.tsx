"use client";

// Draft system removed (migrated to localStorage autosave).
// Keeping this file as a harmless placeholder so existing imports don't break builds.

export function DraftStatus() {
  return null;
}
